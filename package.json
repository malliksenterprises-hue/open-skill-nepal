{
  "name": "open-skill-nepal-backend",
  "version": "1.0.0",
  "description": "Open Skill Nepal Backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup-gcp": "node -e \"if (process.env.GCP_SERVICE_ACCOUNT_KEY_BASE64) { const fs = require('fs'); const path = require('path'); const keyContent = Buffer.from(process.env.GCP_SERVICE_ACCOUNT_KEY_BASE64, 'base64').toString(); const keyPath = path.join(__dirname, 'service-account.json'); fs.writeFileSync(keyPath, keyContent); fs.chmodSync(keyPath, 0o400); console.log('✅ Service account key created:', keyPath); } else { console.log('⚠️ GCP_SERVICE_ACCOUNT_KEY_BASE64 not found - running in mock mode'); }\"",
    "prestart": "npm run setup-gcp",
    "predev": "npm run setup-gcp",
    "clean-gcp": "node -e \"const fs = require('fs'); const path = require('path'); const keyPath = path.join(__dirname, 'service-account.json'); if (fs.existsSync(keyPath)) { fs.unlinkSync(keyPath); console.log('🗑️  Removed service account key'); }\""
  },
  "dependencies": {
    "@google-cloud/storage": "^7.0.0",
    "axios": "^1.13.2",
    "bcryptjs": "^2.4.3",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.3",
    "multer": "^1.4.5-lts.1",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "keywords": [
    "education",
    "nepal",
    "api",
    "backend"
  ],
  "author": "Open Skill Nepal",
  "license": "MIT"
}
